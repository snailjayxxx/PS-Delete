# Security

- 本地核心默认只监听 `127.0.0.1:18780`。
- API Key 优先保存在 macOS Keychain 或 Windows Credential Manager。
- 如果系统凭据服务不可用，核心会退回到权限为 600 的本地配置文件；插件界面会继续正常工作，但用户应保护自己的系统账户。
- 图片只发送给用户选择的 AI 服务商，不经过 SnailJOSS 服务器。
- 日志不应包含图片内容或 API Key。提交问题前请自行检查日志。
- 覆盖物清理只应用于用户拥有或已获授权处理的图片。
